import { IonItem, IonLabel } from "@ionic/react";

export default function ResetTags() {
  return (
    <IonItem button onClick={() => {}} detail={false}>
      <IonLabel color="primary">Reset Tags</IonLabel>
    </IonItem>
  );
}
